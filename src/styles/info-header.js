import {StyleSheet} from 'react-native';

export const InfoHeader = StyleSheet.create({
  header: {
    fontSize: 18,
    fontFamily: 'Montserrat-bold',
    letterSpacing: -0.01,
    marginBottom: 5,
  },
});
